<IfModule mod_rewrite.c>
    RewriteEngine on
    RewriteRule  ^$ public/    [L]
    RewriteRule  ((?s).*) public/$1 [L]
</IfModule>
#<IfModule mod_rewrite.c>
#    RewriteEngine On
#    RewriteRule ^$ public/     [L]
#    RewriteRule (.*) public/$1 [L]
#</IfModule>
